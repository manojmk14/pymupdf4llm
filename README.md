pymupdf4llm is a opensource python library, its a PDF reader for extrating complex data from PDF files in the form of text, tables and images.
This data can be extracted as Markdown files which can be imported into Vector databases for using in LLM /RAG models.
Just install "pip install pymupdf4llm" and enjoy!
